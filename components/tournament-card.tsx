import Link from "next/link";
import { CalendarDays, Trophy, Users } from "lucide-react";
import { Tournament } from "@/lib/data";
import { formatCurrency } from "@/lib/utils";
export function TournamentCard({t}:{t:Tournament}) { return <Link href={`/tournaments/${t.id}`} className="group panel overflow-hidden transition hover:-translate-y-1 hover:border-cyan/30"><div className={`h-24 bg-gradient-to-br ${t.color} p-4`}><span className="rounded-full bg-black/20 px-2 py-1 text-[10px] font-bold uppercase tracking-wider">{t.status}</span></div><div className="p-5"><p className="text-xs font-bold text-cyan">{t.game}</p><h3 className="mt-1 font-bold group-hover:text-cyan">{t.name}</h3><p className="mt-1 text-xs text-slate-400">{t.mode}</p><div className="mt-5 grid grid-cols-2 gap-3 border-t border-white/10 pt-4 text-xs"><span className="flex items-center gap-1 text-slate-400"><Trophy size={14} className="text-warning"/>{formatCurrency(t.prize)}</span><span className="flex items-center gap-1 text-slate-400"><Users size={14}/>{t.teams}</span><span className="col-span-2 flex items-center gap-1 text-slate-400"><CalendarDays size={14}/>{t.date}</span></div></div></Link>; }
