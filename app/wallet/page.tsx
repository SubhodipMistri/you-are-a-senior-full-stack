import { ArrowDownLeft, ArrowUpRight, CreditCard, Gift, Plus } from "lucide-react";
import { DashboardShell } from "@/components/dashboard-shell";
import { Card, PageHeader } from "@/components/ui";
import { formatCurrency } from "@/lib/utils";
export default function Wallet(){const tx=[['Tournament prize','Neon Clash Season 11','+ ₹1,500',true],['Tournament entry','Rift Rivals Cup','- ₹149',false],['Wallet deposit','UPI payment','+ ₹500',true]];return <DashboardShell><PageHeader eyebrow="Payments & payouts" title="Your wallet"><button className="btn-secondary">Transaction history</button></PageHeader><Card className="relative overflow-hidden bg-gradient-to-br from-blue-900/80 to-slate-900 p-7"><CreditCard className="absolute -right-3 -bottom-5 h-40 w-40 rotate-12 text-cyan/10"/><p className="text-sm text-blue-200">Available balance</p><p className="mt-2 text-4xl font-black">{formatCurrency(2480)}</p><p className="mt-2 text-sm text-blue-200">+ ₹320 earned this month</p><div className="mt-7 flex gap-3"><button className="btn-primary"><Plus size={17}/> Add funds</button><button className="btn-secondary">Withdraw</button></div></Card><div className="mt-6 grid gap-6 md:grid-cols-2"><Card className="p-6"><div className="flex items-center gap-3"><Gift className="text-cyan"/><div><h2 className="font-bold">Referral earnings</h2><p className="text-xs text-slate-400">Share your code, earn 5%.</p></div></div><p className="mt-5 text-2xl font-black">₹680</p><button className="mt-4 text-sm font-bold text-cyan">Copy referral code</button></Card><Card className="p-6"><h2 className="font-bold">Promo code</h2><p className="mt-2 text-sm text-slate-400">Have a reward waiting?</p><div className="mt-4 flex gap-2"><input className="min-w-0 flex-1 rounded-lg border border-white/10 bg-white/5 px-3 text-sm outline-none" placeholder="Enter code"/><button className="btn-primary">Apply</button></div></Card></div><Card className="mt-6 p-6"><h2 className="font-bold">Recent transactions</h2>{tx.map(([a,b,c,positive])=><div className="mt-4 flex items-center justify-between border-t border-white/10 pt-4" key={a as string}><span className="flex items-center gap-3"><span className="rounded-lg bg-white/5 p-2">{positive?<ArrowDownLeft className="text-success" size={17}/>:<ArrowUpRight className="text-warning" size={17}/>}</span><span><b className="block text-sm">{a as string}</b><small className="text-slate-500">{b as string}</small></span></span><b className={positive?'text-success':'text-white'}>{c as string}</b></div>)}</Card></DashboardShell>}
