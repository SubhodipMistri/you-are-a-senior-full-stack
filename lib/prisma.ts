/**
 * Database adapter placeholder.
 *
 * The public demo deliberately has no database connection. When production
 * database work begins, replace this with a generated PrismaClient singleton
 * after DATABASE_URL has been configured in the host environment.
 */
export const prisma = null;
